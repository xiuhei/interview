请根据以下面试上下文和回答分析结果，做出面试决策。

{{ context }}

请输出以下 JSON（不要输出任何其他内容）：

```json
{
  "decision": "follow_up | switch_topic | wrap_up",
  "reason": "简短决策理由",
  "target_competency": "下一个能力维度（switch_topic时使用，否则为空）",
  "followup_type": "deepen_detail | verify_authenticity | redirect_back | ask_for_example | quantify_result | none"
}
```

决策规则：
1. follow_up: 当前回答空泛、偏题、真实性存疑时追问
2. switch_topic: 当前维度已获取足够信息，切换到未覆盖的能力维度
3. wrap_up: 只有在达到最小样本后且继续追问增量价值很低时才允许结束；达到题量上限时必须结束

强约束：
- 少于 3 题不能因为低分直接结束
- 达到 7 题必须结束
- 3 题及以上，如明显不符合且继续追问价值很低，可 wrap_up
- 5 题及以上，如已经符合且信息充分、继续追问价值很低，可 wrap_up

优先级：
- 如果当前回答有明显问题需要追问 → follow_up
- 如果当前维度已充分且还有未覆盖维度 → switch_topic
- 如果对同一维度已追问2次以上 → switch_topic
- 如果满足上述提前结束约束或达到上限 → wrap_up
